import React from 'react'

export const Logo = ({isMenuActive}) => (
    <div className={"logoContainer" + isMenuActive}>
        <a href="#">Tarjetos de credito.com.yu</a>
    </div>
)
export default Logo